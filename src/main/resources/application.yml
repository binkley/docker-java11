management:
  endpoint:
    health:
      show-details: 'always'
  endpoints:
    web:
      exposure:
        include: '*'

spring:
  application:
    name: 'my-example'
  main:
    banner-mode: 'off'

---

spring:
  profiles: 'demo'
  application:
    name: 'bobs-yer-unkel'
